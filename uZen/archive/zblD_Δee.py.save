from reflex_engine.logics.encr_flsh_modules import ENCR_Δvn

class ZBLD_Δee:
    def __init__(self):
        self.builder = ENCR_Δvn()

    def issue_tag(self, icon: str, cmid: str, cam_entropy: str):
        tag_bundle = self.builder.compile(icon, cmid, cam_entropy)
        print(f"[ZBLD.Δee] Issued tag for {cmid}")
        return tag_bundle

